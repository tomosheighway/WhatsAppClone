import { StyleSheet } from 'react-native';

export default StyleSheet.create({

  title: {
    fontWeight: 'bold',
    fontSize: 20,
  },
  messageContainer: {
    maxHeight: 500,
    padding: 10,
  },
});
